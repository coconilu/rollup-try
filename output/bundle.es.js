import { throttle } from 'lodash';

const Button = 'Button';

console.log(Button);

const throttleFn = throttle(() => {
  console.log("throttle");
});

const rollup = rollup;

export { rollup, throttleFn };
